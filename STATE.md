# STATE.md — 文獻分診台 LitDesk

**最後更新**：2026-08-16
**版本**：v1.0
**分支**：`claude/litdesk-literature-triage-2kww9t`

---

## 現況

M1 / M2 / M3 的功能面全部實作完成，五條驗收標準以自動化測試驗證通過。
尚未經過真實 API 的實測（見「下一步」）。

| 里程碑 | 完成定義 | 狀態 |
|---|---|---|
| M1 單篇 DOI → 卡片 | 隨機 10 篇實測，metadata 正確率 ≥ 8/10 | 程式完成，**真實實測未做** |
| M2 批次 + 證據庫 | AC-1、AC-3、AC-5 通過 | ✅ 通過（stub 測試） |
| M3 對質台 | AC-4 通過 | ✅ 通過（stub 測試） |

---

## 檔案

| 檔案 | 內容 |
|---|---|
| `litdesk.html` | 整個應用。純前端單檔，無後端、無帳號、無外部相依。 |
| `test_litdesk.mjs` | 驗收測試。以 Playwright 開檔，替換模型呼叫層，63 條檢查。 |

執行測試：

```
npm install playwright
node test_litdesk.mjs
```

不需要 API key，不打網路。

---

## 驗收標準對照

| AC | 內容 | 狀態 | 驗證方式 |
|---|---|---|---|
| AC-1 | 一次貼入 ≥10 個 DOI／連結逐筆處理；單筆失敗不中斷整批，失敗項目標明原因 | ✅ | 12 筆輸入含 4 種失敗（找不到／付費牆／非研究論文／連線例外），12 張卡片全數產出，4 張標明原因 |
| AC-2 | 每篇輸出分診結果等欄位，抓不到就填「未取得」，不得推測或杜撰數字 | ✅ | 缺欄位一律「未取得」；無 `source_quote` 的效果量被丟棄且不出現在 DOM；僅標題的紀錄不給任何效果量 |
| AC-3 | 關閉重開後證據庫仍在，且能以關鍵字搜尋 | ✅ | 新分頁重載後 8 筆仍在；搜尋涵蓋標題／備註／標籤／族群／介入；分診與年份、標籤篩選可用 |
| AC-4 | 勾選 ≥3 篇後產出矛盾點清單，每個矛盾點標明來自哪幾篇 | ✅ | 5 條輸入中 2 條無合法篇號者被丟棄；其餘 3 條均渲染「出處：[n]」 |
| AC-5 | CSV 以 UTF-8 BOM 輸出，Windows Excel 直接開啟中文不亂碼 | ✅ | 下載檔前三個位元組為 `EF BB BF`；CRLF 換行；中文表頭與內容比對正確 |

---

## 與 PRD 的差異（刻意為之）

### 1. `max_tokens` 不是 1000，而是 8000（分診）／12000（對質）

PRD §7 寫 `max_tokens: 1000`。實作沒有照做，原因是這個數字會讓功能直接壞掉：

- `max_tokens` 是 **thinking + 回應文字的總上限**，不是回應長度上限。
- Claude Opus 5 預設開啟 adaptive thinking（省略 `thinking` 參數即為開啟），
  P1 又要跑 web search 的多輪往返 —— 1000 個 token 會在思考階段就被截斷，
  拿不到任何 JSON。
- 關掉 thinking 不是好選項：thinking 關閉時模型偶爾會把工具呼叫寫進純文字，
  該次呼叫靜默地不會執行，而 P1 完全依賴 web search。

改以 PRD 原本的意圖（控制輸出長度）達成同一目的：
`results` 上限 3 條寫死在程式裡（`sanitizeResults`），prompt 要求各欄位精簡。
成本控制改用 `effort`（預設 `medium`，設定頁可調）。

### 2. `window.storage` 有 fallback

PRD §6 指定 `window.storage`。實作是一層 adapter：
有 `window.storage`（含 async 介面）就用它，沒有就退回 `localStorage`。
單一鍵 `litdesk:library` 存整個陣列的設計照 PRD 不變。

### 3. 分診失敗的原因用 `fetch_status` 欄位承載

PRD 只說失敗項目需標明原因（找不到／付費牆／非研究論文），沒有指定怎麼傳。
實作在 P1 的輸出加了 `fetch_status` 與 `fetch_note` 兩個欄位承載這件事，
資料模型其餘欄位維持 PRD 原樣。

---

## 待決問題（PRD §12）現況

1. **效果量單位跨篇不一致是否需要「單位不可比」旗標？**
   目前沒做獨立旗標。P2 的 `不可比` 類型涵蓋「測量工具不同」，
   單位差異會落在這一類。等真實使用時看這樣夠不夠。
2. **證據庫需不需要專案／主題分組？**
   目前只有標籤。標籤 + 搜尋 + 分診／年份篩選先用著，不夠再說。
3. **是否保留每篇原始 JSON 回應？**
   **沒有保留。** 只存規範化後的資料模型。若之後要回溯 prompt 品質，
   要在 `normalizeCard` 前把 raw 存進紀錄（會增加儲存體積）。

---

## 下一步

1. **M1 真實實測**：拿 10 篇真的 DOI 跑一次，數 metadata 正確率。
   低於 8/10 就照 kill criteria 放棄 DOI 輸入，改以貼摘要為主要入口。
2. **M2 kill criteria 計時開始**：連續兩週沒主動打開就結案，不留半成品。
3. 真實實測後回頭看 P1 的分診規則是否需要調整
   （被手動覆寫的紀錄有 `verdict_overridden: true`，那就是調 prompt 的訊號）。
