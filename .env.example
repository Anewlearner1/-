# 投資團隊（team.py）預設不需要 API key，走本機 claude login 的訂閱額度。
# 下面這把金鑰只有 main.py 的每小時大盤分析、或 TEAM_BACKEND=api 時才需要。
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional: Discord Webhook URL for push notifications
# How to get it:
#   1. Open your Discord server → 頻道設定 → 整合 → Webhooks → 建立 Webhook
#   2. 複製 Webhook URL，貼到這裡
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN

# Optional: directory to save reports (default: ./reports)
REPORT_DIR=./reports

# Optional: set to "1" to only run during Taiwan market hours (Mon-Fri 09:00-13:30 CST)
MARKET_OPEN_ONLY=0

# Optional: 投資團隊的 LLM 後端 claude_cli (預設，免 API key) | api
TEAM_BACKEND=claude_cli

# Optional: 投資團隊討論的思考強度 low | medium | high | xhigh | max (預設 high)
TEAM_EFFORT=high
